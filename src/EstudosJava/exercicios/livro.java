package EstudosJava.exercicios;

public class livro {
    public static void main(String[] args) {
        System.out.println("O primerio de muitos códigos escritos em " + args[0] + "!" + args[1]);
    }
}

// o que eu aprendi aqui basicamente foi sobre o bytecode, que é o código compilado do Java, e como ele é executado pela JVM (Java Virtual Machine).
// O código acima define uma classe chamada "livro" dentro do pacote "EstudosJava.exercicios".
// eu so quis demonstrar tipo
// case sensitive tbm significa que o Java diferencia letras maiúsculas de minúsculas, então "Levy" e "lEVY" são considerados diferentes. isso serve diretamente pra cada caso no código que eu digitar.